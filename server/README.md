# server — Spring Boot skeleton (placeholder)

> Este módulo es un esqueleto para futuras integraciones (pagos, webhooks). Aún **no implementa** lógica ni endpoints.
> Cuando se habilite, se recomienda agregar un endpoint `/health` que devuelva `200 OK`.

## Sugerencia de estructura (Maven)
```
server/
  └─ README.md
  └─ pom.xml             # placeholder
  └─ src/main/java/...   # (vacío por ahora)
  └─ src/main/resources/ # (vacío)
```
